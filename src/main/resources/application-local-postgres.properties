spring.datasource.url=${DATABASE_URL:jdbc:postgresql://localhost:5432/cardapio}
spring.datasource.username=${DATABASE_USERNAME:cardapio}
spring.datasource.password=${DATABASE_PASSWORD:cardapio}
spring.datasource.driver-class-name=org.postgresql.Driver

api.security.token.secret=${JWT_SECRET:dev-only-secret-change-before-production-32-chars}
api.security.cookie.secure=${COOKIE_SECURE:true}
api.security.cookie.same-site=${COOKIE_SAME_SITE:None}

supabase.url=${SUPABASE_URL:http://localhost:54321}
supabase.key=${SUPABASE_KEY:dev-only-supabase-key}
supabase.bucket=${SUPABASE_BUCKET:cardapio-imagens}
